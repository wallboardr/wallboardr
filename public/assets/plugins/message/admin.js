define([], function () {
  'use strict';
  var plugin = {};
  plugin.config = {
    name: 'message',
    humanName: 'Local message',
    templateName: 'localMessage',
    centered: true
  };

  return plugin;
});