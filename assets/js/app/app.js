define(['angular'], function (angular) {
    'use strict';

    return {
        bootstrap: function () {
            if (window.console) {
                window.console.log('Tower, this is Ghost Rider requesting a flyby.')
            }
        }
    };
});